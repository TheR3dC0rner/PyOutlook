import outlook_lib
import sys,getopt




def main(argv):
    outputfile = ''
    action = ''
    try:
        opts,args = getopt.getopt(argv,

#session = outlook_lib.init_session()
#inbox = session.GetDefaultFolder(6)
#print inbox
    
